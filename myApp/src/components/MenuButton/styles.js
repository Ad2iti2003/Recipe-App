import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  btnClickContain: {
    flexDirection: 'row',
    padding: 5,
    marginVertical: 5,
  },

  btnContainer: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
  },

  btnIcon: {
    width: 25,
    height: 25,
  },

  btnText: {
    fontSize: 16,
    marginLeft: 10,
  },
});

export default styles;
